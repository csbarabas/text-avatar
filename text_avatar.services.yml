services:
  text_avatar.services:
    class: Drupal\text_avatar\TextAvatarServices
    arguments: ['@file.repository', '@config.factory', '@file_system', '@entity_type.manager', '@stream_wrapper.public']
